# 금연존 지킴이 — 개발 로드맵 (11주)

> 개발 작업만 정리한 버전. 행정(승인·안내판·의견 수렴)과 구매는 별도 트랙으로 관리하고, 여기에는 코드·모델·시스템 작업만 담는다.

## 개발 순서 개요

```
데이터 → 모델·모션 규칙 → 파이프라인 통합 → 오탐 튜닝
→ 서버 연동 → 포팅·대시보드 → 조립 → 현장 튜닝 → 평가
```

1~6주차는 전부 노트북+웹캠 환경에서 개발한다. 라즈베리파이는 7주차 포팅에서 처음 등장한다.

---

## 1주차 — 개발 환경 준비

- 저장소 세팅: 폴더 구조(`edge/ server/ dashboard/ training/ docs/`), `.gitignore`, 이슈 템플릿, 로드맵을 이슈로 등록
- Python 환경 구성: `ultralytics`, OpenCV, MediaPipe 설치 확인, Colab GPU 연결 테스트
- 웹캠으로 기본 YOLO 추론 돌려보기 (환경 검증)

## 2주차 — 데이터셋 구축

- Roboflow 프로젝트 생성, 공개 데이터셋(cigarette/smoking) 수집·업로드
- 클래스 `cigarette` 단일(0번)로 통일 — Modify Classes 전처리 사용
- 자체 촬영 1차: 설치 예정 각도·높이·거리에서 흡연 연출 + 함정 동작(커피·통화·전자담배) 영상
- **엣지↔서버 이벤트 JSON 스키마 확정** (예: `{device_id, timestamp, confidence, event_type}`)

## 3주차 — 첫 모델과 모션 규칙

- 영상에서 1~2fps 프레임 추출 → 라벨링 완료
- train/val 분리는 **촬영 세션 단위로** (같은 영상 프레임이 양쪽에 섞이면 성능 뻥튀기)
- YOLOv8n 1차 학습 (imgsz 640 기본, 960 비교), mAP 벤치마크 — **표준 구조 유지 (Hailo Model Zoo 검증 경로), 커스텀 레이어 금지**
- **파이 CPU 벤치마크 선행 (반나절)**: 1차 모델을 NCNN/TFLite로 변환해 파이5 CPU-only FPS 실측 → 7주차 포팅이 막힐 경우의 플랜 B(CPU 추론) 성립 여부를 미리 확인
- 포즈 기반 손-입 반복 모션 감지 프로토타입: 손목-입 거리 정규화(어깨너비 기준) → "한 모금" 사이클 카운트

## 4주차 — 파이프라인 통합 ★ 중간 마일스톤

- 통합: 사람 감지 → 모션 1차 게이트 → 담배/연기 2차 확인 → 시간 필터(10초 윈도 60%)
- **웹캠 실시간 데모 v1 완성**
- 음성 송출 상태 머신: 이벤트 확정 → 1차 안내 mp3 재생 → 30초 후 재감지 시 2차 안내 → 쿨다운

## 5주차 — 오탐 튜닝

- 함정 동작 벤치마크: 흡연 10회 + 커피·통화·전자담배 등 각 10회 → 정밀도/재현율 표
- 부족한 케이스 2차 촬영 → 재학습
- 신뢰도 임계값·윈도 파라미터 튜닝 (정밀도 우선)
- 데모 v1 문제 수정

## 6주차 — 서버 연동

- FastAPI 이벤트 API (`POST /events`, `GET /events`, `POST /heartbeat`) + SQLite
- 엣지 앱에 이벤트 전송 모듈: 실패 시 로컬 큐잉 후 재시도
- 장치 하트비트 (1분 주기 상태 보고)
- 노트북 기준 엣지↔서버 통합 테스트

## 7주차 — 포팅 + 대시보드 (병렬)

**포팅 트랙**
- 파이5 OS 설치, Hailo SDK 세팅, 카메라(RTSP)·스피커 구동 확인
- 학습 모델 Hailo 컴파일(.hef) → 파이에서 전체 파이프라인 구동, FPS 실측 (목표 10, 마지노선 5)
- systemd 서비스 등록 (재부팅 자동 복구)
- ⚠️ **주 중반 체크포인트**: Hailo 컴파일이 막혀 있으면 → 플랜 B: NCNN/TFLite CPU-only 추론으로 전환 (3주차 벤치마크로 FPS 이미 확인됨) → 그것도 미달이면 플랜 C: 노트북을 현장 두뇌로. 어느 경우든 8주차 설치 일정은 지킨다

**대시보드 트랙**
- Streamlit: 시간대·요일 히트맵, 일별 감지 추이, 장치 상태(하트비트) 표시

## 8주차 — 조립·설치·섀도 모드

- 현장 장치 조립: 파이(또는 노트북) + IP 카메라 + 스피커, 전원·네트워크 결선
- 현장 설치 후 **섀도 모드 가동** — 방송 없이 감지 로그만 수집
- 원격 접속(SSH) 확보, 로그 수집 확인

## 9주차 — 현장 튜닝, 방송 활성화

- 섀도 로그로 실환경 정밀도 검증 (실제 흡연 vs 감지 대조)
- 실환경에 맞춰 임계값 최종 조정
- 정밀도 확보 확인 후 **방송 기능 ON**
- 현장 이슈 대응: 역광·그림자 오탐, RTSP 끊김 재연결, 음량 조정

## 10주차 — 운영·지표 수집

- 파일럿 운영 지속, 지표 자동 집계: 방송 정밀도, 시간대별 감지 분포
- 안내 후 60초 내 중단율 표본 관찰 기록
- README 완성: 데모 GIF, 아키텍처 그림, 실행 방법

## 11주차 — 평가·마무리 ★ 완성 마일스톤

- 지표 종합: 정밀도/재현율/FPS/중단율 최종표
- 한계·향후 과제 정리 (야간, 다지점, 전자담배 기기 감지 등)
- 코드 정리, 최종 시연 준비

---

## 산출물 체크리스트

| 주차 | 산출물 |
|---|---|
| 3 | 학습된 YOLO 모델 v1 + 벤치마크 표 + 파이 CPU FPS 실측치 (플랜 B 성립 여부) |
| 4 | 웹캠 실시간 데모 v1 |
| 5 | 오탐 벤치마크 표 + 모델 v2 |
| 6 | 이벤트 API + 엣지 연동 |
| 7 | 파이에서 도는 파이프라인 (or 플랜 B 결정) + 대시보드 |
| 8 | 현장 설치 완료, 섀도 로그 수집 중 |
| 9 | 방송 활성화된 운영 시스템 |
| 11 | 최종 지표·시연·정리된 저장소 |
