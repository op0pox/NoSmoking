# CLAUDE.md — 금연존 지킴이 프로젝트 컨텍스트

이 저장소 루트에 `CLAUDE.md`로 두면 Claude Code가 매 세션 자동으로 읽는다.

## 프로젝트 개요

**금연존 지킴이**: 교내 금연구역에 설치한 카메라가 흡연 행동을 실시간 감지하면 스피커로 안내 음성을 송출하고, 서버에는 익명 이벤트만 기록하는 엣지 AI 시스템. 캡스톤 프로젝트, 2~3인 팀, 11주 개발, 최종 목표는 교내 1개소 실기기 파일럿.

- 목적은 **계도**이지 단속·처벌이 아니다. 사람을 식별하지 않는다.
- 핵심 설계 원칙: **영상 무저장** — 프레임은 엣지 디바이스에서 추론 직후 메모리에서 폐기한다. 저장·전송·녹화 기능을 코드에 만들지 않는다. 서버로 가는 것은 `{시각, 지점 ID, 신뢰도}` 메타데이터뿐이다.
- 이 원칙은 개인정보보호법 제25조(고정형 영상정보처리기기) 대응이자 프로젝트의 차별점이다. **어떤 기능 요청이라도 영상을 저장하거나 서버로 전송하는 코드는 제안·작성하지 말 것.** 녹음도 법적 금지라 오디오 입력 코드도 금지.

## 시스템 구성

```
[엣지: 현장 장치]
IP 카메라(RTSP) → 라즈베리파이5 + Hailo-8L AI Kit → 스피커(안내 음성)
                          │
                          └─ HTTPS로 이벤트 메타데이터만 전송
[서버]
FastAPI + SQLite(→PostgreSQL) → Streamlit 대시보드(시간대 히트맵, 장치 상태)
```

개발(1~6주차)은 노트북+웹캠에서 진행하고, 7주차에 파이로 포팅한다. 포팅 리스크 관리 3단계:

1. **주력**: Hailo 컴파일 (YOLOv8n 표준 구조 유지가 전제 — Hailo Model Zoo 검증 경로)
2. **플랜 B**: NCNN/TFLite로 파이5 CPU-only 추론. 모션 게이트 구조 덕에 무거운 YOLO는 의심 구간에만 돌므로 실효 부하가 낮다. 3주차에 1차 모델로 CPU 벤치마크를 미리 수행해 이 경로의 FPS를 확인해둔다.
3. **플랜 C**: 노트북을 현장 두뇌로 파일럿 진행, 파이 포팅은 향후 과제.

## 감지 파이프라인 (핵심 로직)

3단계 + 시간 필터. 순서가 중요하다.

1. **사람 감지**: COCO 사전학습 YOLOv8n. 사람 없으면 이후 단계 스킵.
2. **1차 게이트 — 손-입 반복 모션** (규칙 기반, 학습 없음):
   MediaPipe Pose로 손목·입 좌표 추출 → 거리를 어깨너비로 정규화 →
   "정규화 거리 < 0.35가 1~6초 유지 후 복귀"를 puff 1회로 카운트 →
   60초 윈도에 3회 이상이면 흡연 의심. 연초·전자담배 공통으로 잡힌다.
3. **2차 확인 — 객체/연기**: 의심 구간에서만 커스텀 YOLO 담배 감지
   (+ 향후 연기·증기 감지) 실행. 신뢰도 점수를 모션 점수와 합산.
4. **시간 필터**: 10초 윈도 중 흡연 판정 비율 ≥ 60%일 때만 이벤트 확정.
5. 이벤트 확정 → 1차 안내 방송 → 30초 후 재감지 시 2차 방송 → 쿨다운.

설계 기조: **정밀도 우선**. 비흡연자에게 방송이 나가는 오탐이 최악의 실패다. 임계값은 보수적으로, 미탐은 허용한다. 주요 오탐 시나리오(커피, 통화, 물병, 전자담배 아닌 손동작)는 벤치마크 셋으로 관리한다.

## 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 감지 모델 | Ultralytics YOLOv8n (imgsz 640) | **표준 구조 유지 — Hailo Model Zoo에 있는 그대로.** 커스텀 레이어·구조 변경 금지 (컴파일 실패 리스크) |
| 포즈 | MediaPipe Pose | 학습 불필요, 규칙 기반 판정 |
| 엣지 앱 | Python + OpenCV, systemd 서비스 | RTSP 입력: cv2.VideoCapture |
| 엣지 가속 | Hailo SDK (.hef 컴파일) | 주력 경로. 플랜 B: NCNN/TFLite로 파이5 CPU-only 추론 (3주차에 벤치마크 선행) |
| 음성 | 사전 녹음 mp3 재생 | 실시간 TTS 안 씀, 마이크 없음 |
| 서버 | FastAPI + SQLite (개발) | POST /events, POST /heartbeat |
| 대시보드 | Streamlit | React 아님 (범위 축소 결정) |
| 데이터 | Roboflow (클래스: cigarette 단일, 0번) | 저장소에 데이터셋 커밋 금지 |

## 저장소 구조

```
edge/          # 파이(현장 장치)에서 돌아갈 코드: 감지 파이프라인, 음성, 이벤트 전송
server/        # FastAPI, DB
dashboard/     # Streamlit
training/      # 학습 스크립트, 전처리, data.yaml, download_dataset.py
docs/          # 기획서, 가이드, 회의록
```

## 코딩·협업 규칙 (docs/깃허브-운영-규칙.md 요약)

- 모든 작업은 이슈로 시작 → 이슈 번호 브랜치(`12-pose-hand-mouth`) → PR에 `Closes #12` → Squash merge. main 직접 푸시 금지. 머지된 브랜치는 보존하되 재사용 금지.
- 커밋 메시지: `feat:` `fix:` `docs:` `refactor:` `chore:` `exp:` + 한글 설명.
- **커밋 금지**: 데이터셋(이미지·영상·라벨), 모델 가중치(.pt/.onnx/.hef), `.env`, `runs/`. 자체 촬영 영상은 팀원 얼굴이 포함된 개인정보다.
- train/val 분리는 반드시 촬영 세션(영상) 단위로. 프레임 단위로 섞으면 성능이 뻥튀기된다.

## 자주 쓸 파라미터 (튜닝 대상, 코드에선 상수로 모을 것)

- 모션: NEAR_THRESHOLD=0.35, MIN_HOLD_SEC=1.0, MAX_HOLD_SEC=6, WINDOW_SEC=60, MIN_PUFFS=3
- 시간 필터: 윈도 10초, 흡연 판정 비율 60%
- 성능 목표: 이벤트 정밀도 ≥95%, 재현율 ≥80%, 엣지 ≥10 FPS(마지노선 5), 감지→방송 ≤2초

## Claude Code에게

- 기능을 추가할 때 위 "영상 무저장" 원칙과 정밀도 우선 기조를 항상 지켜라. 디버깅 목적이라도 프레임을 디스크에 쓰는 코드는 기본적으로 만들지 말고, 꼭 필요하면 명시적 `--debug-save` 플래그 뒤에 숨기고 경고 주석을 달아라.
- 의존성은 가볍게 유지하라. 파이5에서 돌아야 한다. 무거운 프레임워크 추가 전에 대안을 먼저 검토하라.
- 모델 관련 코드에서 YOLOv8n의 네트워크 구조를 바꾸는 제안(레이어 추가·교체, attention 모듈 등)은 하지 마라. 구조 변경은 Hailo 컴파일 실패로 직결된다. 성능 개선은 데이터·증강·임계값 튜닝으로 접근하라.
- 임계값·매직넘버는 파일 상단 상수나 config로 모아라. 5주차와 9주차에 집중 튜닝 예정이다.
- 새 모듈은 노트북(웹캠)과 파이(RTSP) 양쪽에서 돌 수 있게 입력 소스를 주입 가능하게 설계하라.
- 한글 주석 환영. 팀원은 파이썬 중급, 임베디드 경험은 적다 — 트릭보다 읽기 쉬운 코드를 우선하라.
